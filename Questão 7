#include <stdio.h>

int main(){

    int mat[4] = {10,20,30,40}, *p, x;

    //p = mat + 1; // resultado p = 20
    //p = mat++; // deu erro
    //p = ++mat; // deu erro
    //x = (*mat)++; // deu erro

    for(int i = 0; i <= 3; i++){
        printf("mat[%d] = %d ", i, mat[i]);
    }
    
    printf("x =  %d \n", x);
    printf("p =  %d \n", *p);

    return 0;
}
